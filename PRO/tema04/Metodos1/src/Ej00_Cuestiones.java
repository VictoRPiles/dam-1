public class Ej00_Cuestiones {
	/**
	 * 1.-Dado el siguiente prototipo
	 * float f(int a, float b, int c)
	 * y la siguiente declaración de variables:
	 * int a,b,c,d;
	 * float x,y,z;
	 * ¿cuál de las siguientes llamadas a la función es correcta?
	 * a) x=f(a,b,c);
	 * b) y=f(a,x,y);
	 * c) z=f(a,x,b);
	 * d) a=f(a,b,c)
	 * <p>
	 * RESPUESTA
	 * La opción A y C son correctas, en la B pasamos un float como tercer parámetro y en la D almacenamos un float en
	 * la variable de tipo entero 'a'.
	 */
	/**
	 * 2.- Realizar la traza del siguiente programa, indicando al final cual es el mensaje que
	 * aparece en pantalla.
	 * <p>
	 * RESULTADO
	 * s1 = 32
	 * s2 = 114
	 */
}
